import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>🏨 Banco de Fornecedores</Text>
      <Text style={styles.subtitle}>App funcionando! 🎉</Text>
      <Text style={styles.emoji}>📱 Escaneie o QR Code no Expo Go</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#F7F9FC'
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#2E5E4F'
  },
  subtitle: {
    fontSize: 16,
    color: '#666',
    marginTop: 10
  },
  emoji: {
    fontSize: 14,
    color: '#999',
    marginTop: 20
  }
});
